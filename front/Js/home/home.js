// Adiciona um ouvinte de evento que espera o carregamento completo do DOM (estrutura HTML)
// Isso garante que o código dentro só execute depois que toda a página estiver pronta
document.addEventListener('DOMContentLoaded', () => {
  // Exibe uma mensagem no console indicando que a aplicação foi inicializada com sucesso
  console.log('Aplicação inicializada com sucesso');
});
